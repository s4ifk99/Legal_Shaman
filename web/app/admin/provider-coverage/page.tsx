import ProviderCoverageClient from "./provider-coverage-client";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function ProviderCoverageAdminPage() {
  return <ProviderCoverageClient />;
}
